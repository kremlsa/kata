```
from flask import Flask, request

app = Flask(name)

@app.route("/login", methods=["POST"])
def login():
    username = request.form.get("username")
    password = request.form.get("password")

    if username == "admin" and password == "admin123":
        return {"status": "ok"}
    return {"status": "fail"}, 401

if name == "main":
    app.run(debug=True)
```

# Вопросы:

### Где здесь может быть уязвимость?  
Broken authentification  
### Как её можно эксплуатировать?  
BruteForce  
### Какие последствия?  
Несанкционированный доступ с вытекающими последствиями (нарушение всех трех свойств информации) (если по нашей школе)  
Эскалация привилегий (если на манер OWASP)  
### Как исправить?  
не сравнивать пароли в открытом виде (использовать стойкие функции с хешированием)  
не хардкодить пароли в открытом виде в коде    
обеспечить хранение паролей с солью  
обеспечить парольную политику, исключающую возможность формирование таких слабых паролей. Для пользователей с высокими привилегиями можно сделать более жесткую политику (например по количеству символов)  
нужно лимитировать количество запросов на аутентификацию. в случае неудачи нужно повесить таймаут (чтобы осложнить брутфорс). можно по аналогии с нашими требованими просто учетку залочить после n-го количества неудачных попыток входа  
